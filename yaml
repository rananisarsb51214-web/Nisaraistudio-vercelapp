env:
  # ... existing envs ...
  YJS_ENABLED: "true"
  YJS_PORT: "1234"
  WORKSPACE_PATH: "/workspace"

service:
  # ... existing config ...
  ports:
    - name: http
      port: 3000
      targetPort: 3000
    - name: yjs-ws
      port: 1234
      targetPort: 1234